{
  "$schema": "node_modules/lerna/schemas/lerna-schema.json",
  "version": "independent",
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "command": {
    "bootstrap": {
      "loglevel": "verbose"
    },
    "version": {
      "private": false,
      "push": true,
      "conventionalCommits": true
    }
  }
}
