{
  "name": "@ds-ifc/ds-tokens",
  "version": "0.0.1",
  "main": "lib/css/ds-tokens.css",
  "type": "module",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "yarn run clean && yarn compile && yarn start",
    "clean": "rm -rf ./lib",
    "start": "node ./config.js",
    "compile": "tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "style-dictionary": "^3.8.1"
  }
}
